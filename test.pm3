//precision 2
//epsilon 1e-99
//variables x y z

//simplify true

a = 3
b = -7
c = 2

discriminant = b ** 2 - 4 * a * c

return discriminant < 0
return discriminant == 0
return discriminant > 0

sqrt_disc = discriminant ** 0.5
numerator = -b + sqrt_disc
neg_numerator = -b - sqrt_disc
denominator = 2 * a
return numerator / denominator
return neg_numerator / denominator

return -1 == -1